<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="stis.ks2.group2.eventagenda">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_eventku"
        android:hardwareAccelerated="false"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.EventActivity"
            android:noHistory="true" />
        <activity android:name=".activities.InformasiPendaftarActivity"></activity>
        <activity
            android:name=".model.Splash"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.BerandaActivity"
            android:label="@string/title_activity_beranda"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.PertanyaanActivity"
            android:noHistory="true" />
        <activity
            android:name=".activities.AddEventActivity" />
        <activity android:name=".activities.EventKuActivity" />
        <activity
            android:name=".activities.PertanyaanTambahanActivity"
            android:noHistory="true" />
        <activity android:name=".activities.ListerUserActivity"></activity>
        <activity android:name=".activities.ManageEventActivity"></activity>
    </application>

</manifest>